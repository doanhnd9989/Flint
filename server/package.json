{
  "name": "flint-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Auth + system-management API for the Flint workspace (users, feature flags, workspace config).",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "seed": "node --env-file-if-exists=.env db.js --seed"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.8.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "graphql": "^16.14.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^9.0.4",
    "ws": "^8.21.0"
  }
}
