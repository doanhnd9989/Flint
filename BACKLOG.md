# Backlog — Linear feature parity

The 5-minute loop works **top-to-bottom**. Pick the first unchecked item, build
it faithfully to Linear, verify (typecheck + build + browser), then check it off
and log it in `PROGRESS.md`. Add newly discovered Linear features to the bottom.

Priority key: 🔴 core · 🟡 important · 🟢 polish

## ✅ Done (MVP)

- [x] App shell: sidebar, routing, theme (light/dark/system)
- [x] Issues list grouped by status, tabs Active / Backlog / All
- [x] Board (kanban) view with drag-and-drop between columns
- [x] Issue detail: title, description, sub-issues, comments, activity feed
- [x] Right-sidebar property pickers: status, priority, assignee, estimate, labels, project
- [x] Create-issue modal with all properties (⌘↵ to submit)
- [x] Command menu (⌘K): navigate, actions, jump to any issue
- [x] Property pickers as reusable searchable menus
- [x] My Issues, Inbox/notifications, Projects, Project detail, Views, Settings
- [x] Global keyboard shortcuts (⌘K, C, I, M)
- [x] localStorage persistence

## 🔜 To build (in priority order)

- [x] 🟡 **Project detail — Overview tab + Properties sidebar** — soi'd Linear's project page (workspace "Claude Test App", new "Overview Soi Demo" project): the sub-nav is **Overview · Activity · Issues** (Overview default), the main column is a doc (large project icon, **title**, short summary, a **project-update card**, **Description**, **Milestones** list) and a right-hand **Properties** panel — Status, Priority, Lead, Members, Issues count, Dates (Start → Target), Teams, Slack, Labels — plus Milestones + Activity mini-sections. Reproduced the faithful slice we can back with data: rewrote `ProjectDetail` from the header-block + Issues/Updates tabs into **Overview / Activity / Issues**. **Overview** = main column (icon, title, summary, update card linking to Activity, inline-editable Description via `updateProject`, milestones list with `milestoneProgress` bars + `+`) and a right **Properties sidebar** wired to real store actions: **Status** (`SelectMenu` over `PROJECT_STATUS_ORDER` + `ProjectStatusIcon`), **Lead** (`AssigneePicker`), **Members** (keep-open multi-select toggling `memberIds`), **Issues** (`ProgressDonut` + count), **Start/Target** (`DatePicker`), **Teams** (read-only names), **Initiative** (`SelectMenu`). **Activity** = the existing `ProjectUpdates` feed; **Issues** = the existing milestone-grouped list. Header now shows the `ProjectStatusIcon`. Verified live (localhost:5199, MVP Launch): Overview renders 1:1 with Linear (rocket icon, summary, on-track card, Alpha 0/1 / Beta 0/0 milestones, full Properties panel); Status picker opens all six statuses; Issues tab groups by milestone; **console clean after fixing a nested-`<button>` hydration error** (pickers wrap the trigger in their own button, so triggers are now `<span className={triggerCls}>`); `tsc -b` + build green. _(Priority/Labels/Slack rows omitted — Project has no priority/label/slack model; the inline Overview properties row + per-tab deep-linking still TODO; milestone add still uses `prompt()`.)_
- [x] 🔴 **Filtering bar** — add/remove filters (status, assignee, priority, label, project) with active filter chips; wired into `IssuesView`. _(Persist-per-view still TODO.)_
- [x] 🔴 **Issue peek panel** — click a row opens issue in a right-side panel (Linear's split view) without leaving the list; Esc closes. _(Board cards still navigate full-page — could also peek.)_
- [x] 🔴 **Sub-issue progress rollup** — `done/total` + bar on the parent's sub-issue header, a progress-donut badge on parent rows, and a parent breadcrumb on sub-issue detail/peek.
- [x] 🔴 **Bulk selection & actions** — row + group-header checkboxes, floating action bar (status/priority/assignee/label/delete), Esc/Clear/route-change clears. _(Shift-range select and the `X` hotkey still TODO.)_
- [x] 🔴 **Right-click context menu** on issue rows — status, priority, assignee, labels (reusing the property pickers as sub-menus), open in peek / full page, copy id, delete.
- [x] 🔴 **Cycles (sprints)** — cycle view with active/upcoming/past badge, prev/next navigation, scope/started/completed/progress stats + stacked progress bar, and issues-in-cycle grouped by status. Sidebar + ⌘K entry. _(Burndown chart still TODO.)_
- [x] 🟡 **Issue relations** — blocks / blocked-by / related / duplicate-of, stored canonically and shown (with inverses derived) in the issue detail; add via per-kind issue pickers, remove inline.
- [x] 🟡 **Rich-text description editor** — click-to-edit Markdown: headings, bold/italic, inline + fenced code, bullet/ordered/task lists (interactive checkboxes), blockquotes, links, hr. Dependency-free renderer (React nodes, no XSS). _(Full WYSIWYG / slash-commands still TODO.)_
- [x] 🟡 **@mentions** in comments and descriptions — `@`-triggered user autocomplete (arrow/enter/tab/esc), inserts a `@[Name](id)` token rendered as a chip via the Markdown renderer. Comments now render Markdown too.
- [x] 🟡 **Emoji reactions** on comments — reaction pills (emoji + count, highlighted when you reacted, reactor names on hover) and an emoji-picker popover to add; toggling removes your reaction.
- [x] 🟡 **Due-date picker** — month calendar popover (prev/next, Today, Clear) on the issue's Due date property; overdue (red) / due-soon (amber) styling on the property and in issue rows.
- [x] 🟡 **Labels management** in settings — create / rename (inline) / recolor (swatch picker) / delete (with usage count; removes the label from all issues). _(Label groups still TODO.)_
- [x] 🟡 **Custom workflow states** in settings — add / rename / recolor / change type / reorder (within type) / delete (reassigns affected issues). Lives in a "Workflow states" Settings card.
- [x] 🟡 **Issue templates** — a Template picker in the create modal prefills title/description/priority/labels/status; manage templates (create with fields, delete) in a Settings card. Seeded Bug report + Feature request.
- [x] 🟡 **Saved views** — "Save view" in the Issues header captures layout+group+sort+filters as a named view; views open at `/view/:id` (workspace-wide), are editable (edits persist back to the view), and are listed/deletable under "Views".
- [x] 🟡 **Triage** — a per-team queue of incoming issues (`triage` flag) with Accept (into the workflow) / Decline (cancel); inline status/priority/assignee/label pickers, a sidebar entry with a count badge, and an empty state. Triage issues are hidden from the normal issue lists.
- [x] 🟡 **Full-text search** view (`/search`) — substring search across issue id/title/description and project name/description, narrowable with the filter bar, grouped results, and persisted recent searches (click to re-run, Clear). Sidebar "Search" now opens it.
- [x] 🟡 **Manual drag-to-reorder** issues within a list group — dnd-kit sortable rows in `GroupedIssueList`; dropping writes a midpoint `sortOrder` and switches the Issues view to Manual ordering. _(Same-group only; cross-group moves stay on the board / pickers.)_
- [x] 🟡 **Keyboard shortcuts help overlay** (press `?`) — categorized shortcut reference; added `G`-prefixed navigation chords (G I/M/B/C/T/P/V/S) and a ⌘K "Keyboard shortcuts" command. _(Row-level assign/status/priority hotkeys still TODO.)_
- [x] 🟡 **Project milestones** — the project detail groups issues by milestone with per-milestone progress bars (+ "No milestone"); add milestones from the header, delete inline; a Milestone property picker on issues in a milestoned project.
- [x] 🟡 **Project updates** — post health updates (on-track / at-risk / off-track) with a markdown body from an Updates tab; reverse-chronological timeline with delete; the project header shows the latest health badge.
- [x] 🟢 **Roadmap / timeline** view (`/roadmap`) — projects as horizontal bars on a month axis, sized/positioned by start→target dates, colored per project with a progress fill and a "today" marker; click a bar to open the project. Sidebar + ⌘K + `G R`.
- [x] 🟢 **Favorites** — a star button on issue / project / saved-view headers toggles a persisted favorite; starred items appear in a "Favorites" section in the sidebar (click to open).
- [x] 🟢 **List virtualization** — `GroupedIssueList` switches to a windowed renderer (`VirtualIssueList`) above 50 rows: only the rows in the viewport are mounted (verified: 202 issues → 33 DOM rows). Reorder/collapse drop out at that size by design. _(Closes seed issue CLA-5.)_
- [x] 🟢 **Multi-team** polish — a workspace/team switcher dropdown (jump between teams), a "Teams" settings card with per-team membership (add/remove via picker), `Team.memberIds` with a persist `merge` that backfills old workspaces, and seeded Engineering issues + correct per-team issue identifiers.
- [x] 🟢 **Members & roles** — the Settings Members card has a per-member role selector (admin/member/guest, your own locked), remove (not yourself), and a mock invite form that derives a name from the email and adds a Pending member. `User.role` backfilled via the persist merge.
- [x] 🟢 **Notification detail** — Inbox/Snoozed tabs, per-notification snooze (1h / tomorrow / next week) + unsnooze, delete, mark-read-on-open, and a per-type preferences popover; the sidebar badge excludes snoozed.
- [x] 🟢 **Activity types** — every change kind (status/priority/assignee/label/project/milestone/estimate/due/title) is logged with `from`/`to` and rendered as a Linear-style sentence with glyph/chip diffs in a new `ActivityItem` component.
- [x] 🟢 **Git branch name** copy + "Copy issue URL", more issue context actions — `branchName()` (Linear's `handle/cla-123-title-slug`, handle from email local-part) + `issueUrl()` helpers; context-menu rows (Copy issue ID / URL / git branch name) and header buttons on the issue detail + peek. _(Toast feedback on copy still TODO.)_
- [x] 🟢 **Command menu contextual actions** — when viewing an issue (detail route or peek), ⌘K shows an issue-context chip + contextual commands (Assign to…, Assign to me, Change status…, Set priority…, Add to project…, Add labels…, Copy ID/URL/branch) that drill into searchable sub-pages with a check on the current value; Esc/Backspace pops back. _(Set due date sub-page still TODO — it needs the calendar UI.)_
- [x] 🟢 **Empty states & onboarding** polish to match Linear — a reusable `EmptyState` component (centered line-art illustration + muted title + description + optional accent-pill action) with six monochrome SVG illustrations (issues ring, inbox tray, magnifier, card stack, cycle, checkmark), wired into the issue list / My Issues / Inbox (inbox + snoozed) / Search no-results / Cycles / Triage / Projects / Views. _(Onboarding tour / first-run checklist still TODO.)_
- [x] 🟢 **Import / export** issues as JSON/CSV — a Linear-faithful "Import & export" Settings card: **Import** (file picker for `.csv`/`.json`, "creates a copy"), **Export** (Export… → CSV / JSON, downloads immediately). Name-based serializer (`importExport.ts`) so both formats round-trip; CSV is RFC-4180 (quoted fields, embedded newlines). `importIssues` store action resolves team/status/assignee/labels/project/milestone by name, assigns fresh per-team identifiers, logs `created` activity, never overwrites. _(No email-delivered async export — direct download instead, since there's no backend.)_

## Discovered later

_(loop appends new Linear features it notices here)_

- [x] 🟡 **Onboarding tour / first-run checklist** — soi'd Linear: it's not a separate widget but a **"Try"** sidebar section (same header style as Workspace/Your teams) with rows **Import issues** (copy icon), **Invite people** (+), **Connect GitHub** (octocat). Each row navigates to the relevant flow; a hover **×** dismisses it individually; completing the action auto-hides it (Invite people hides once a member is invited / pending); the section disappears when none remain. Dismissals persist (`onboardingDismissed`). _(GitHub has no real integration — routes to Settings.)_
- [x] 🟡 **Burndown chart** for cycles — an SVG burndown on the Cycle page (under the summary/progress bar): a dashed **Ideal** guideline from scope→0 across the cycle's days, the actual **Open** remaining line (steps down per completion, derived from each issue's `completedAt`, area-filled, stops at "today" for in-flight cycles), Y gridlines/ticks, a dashed "today" marker, start/mid/end date labels, and an Open/Ideal legend. Hidden when scope is 0 (upcoming/empty cycles). Seed cy_1 made mid-flight (−7d…+7d) with one completed issue so the chart is illustrative. _(Started-over-time area still TODO — needs per-state-change history; issue-count unit only, no points toggle yet.)_
- [x] 🟢 **Toast feedback on copy** — a bottom-right toast stack (`Toaster` + `lib/toast.ts` non-persisted store) with Linear's exact copy wording (`"CLA-1" copied to clipboard` / `Issue URL copied to clipboard` / `Branch name copied to clipboard. Paste it into your favorite git client.`), dark info-circle icon, × dismiss, 5s auto-dismiss; wired into all copy actions (context menu, ⌘K issue-context, issue detail + peek headers). _(Reusable `toast()` helper available for future actions.)_
- [x] 🟢 **Set due date ⌘K sub-page** — a `dueDate` command-menu sub-page (issue context) matching Linear: placeholder `Try: 24h, 7 days, Feb 9`, quick options **Custom…** / **Tomorrow** / **End of this week** / **In one week** with resolved dates right-aligned, a relative/explicit date parser (`24h`, `7 days`, `Feb 9`, `6/20` → bumps past month-dates to next year), `Custom…` opens the inline `Calendar`, and a **Remove due date** row when set. `Set due date…` (⇧ D) added to the issue-context root. Also fixed input focus persisting across sub-page drill-ins (mouse + keyboard).
- [x] 🟢 **Label groups** in settings — soi'd Linear: the Labels page has **New group** + **New label** buttons; a group is a container row (collapse chevron + multi-dot group glyph + name + child count) whose child labels nest below with tree connectors, then ungrouped labels follow. Reproduced in the Labels settings card: `Label.isGroup`/`groupId`; `createLabelGroup` + `createLabel(name,color,groupId?)` store actions; collapsible groups, per-group "add label" (+) that auto-opens an inline input, inline rename/recolor of group & children, delete-group ungroups its children (non-cascading). Groups are excluded from issue/filter/command-menu label pickers and group-by-label. Seeded a "Type" group (Bug/Feature/Improvement).
- [x] 🟢 **Async / email-style export** — soi'd Linear: clicking **Export…** fires a bottom-right toast titled **"Check your email"** / "Once the export is ready, it will be emailed to you (email)." Reproduced 1:1: the Export card text now reads "…Once the export is available, we'll email you the download link.", choosing a format fires that exact toast, the button shows a **spinner + "Exporting…"** pending state (hint "Preparing CSV export…") for a mocked prepare delay, then a second **"Export ready"** toast appears with a **Download** action (our stand-in for the emailed link, since there's no backend). Extended the toast store/`Toaster` to support an optional **title**, an **action button**, and per-toast **duration**.
- [x] 🔴 **Initiatives** — soi'd Linear's real Initiatives surface (workspace "Claude Test App"): a workspace-level page with **Active / Planned / Completed** tabs, the exact empty-state copy ("Initiatives are larger, strategic product efforts that set the direction of your company…") with a three-bar line-art illustration + "Create new initiative" / "Documentation", and rows showing icon, name, status ring, project count, progress rollup and owner. Built the `Initiative` model (`status`: backlog/planned/active/completed, owner, target date) + `Project.initiativeId`; store actions `createInitiative`/`updateInitiative`/`deleteInitiative`/`setProjectInitiative` with a persist-merge backfill that links the seed projects. `/initiatives` list view, `/initiative/:id` detail (header with status/owner pickers + progress, a Projects list with per-project progress, add/remove projects), a **New initiative** centered modal (icon palette, name, description, status/owner, ⌘↵), a sidebar "Initiatives" entry in Workspace, and ⌘K "Go to Initiatives" / "Create new initiative". Seeded an "H2 Product Launch" initiative over MVP + Mobile.
- [x] 🟡 **Issue subscribers** — surfaced the long-existing `subscriberIds` data model in the UI, mirroring Linear: a **Subscribers** section at the bottom of the issue's right-hand Properties panel (uppercase header) showing stacked subscriber avatars (`-space-x-1`, ring) + "{name}" / "{n} subscribers", opening a searchable multi-select member picker (`SubscriberPicker`, `keepOpen`, checkmarks) to add/remove; and a **Subscribe / Unsubscribe** toggle (bell / bell-off) in the Activity section header that flips the current user's subscription. New `toggleIssueSubscriber(id, userId)` store action; deduped the seed's `subscriberIds` (assignee-is-me produced `['u_me','u_me']`) and rendering. Shared by the full-page detail and the peek panel. _(No subscribe-on-comment / auto-subscribe-on-assign change yet; not logged as a timeline activity, matching Linear.)_
- [x] 🟡 **Display properties** (Display options popover) — soi'd Linear's Display popover on the active-issues view: under Layout / Grouping / Ordering it has a **Display properties** section of toggle pills in the exact order ID · Status · Assignee · Priority · Project · Due date · Milestone · Labels · Links · Time in status · Created · Updated (active = filled, inactive = outlined). Reproduced 1:1: a `DisplayProperty` type + `DISPLAY_PROPERTIES`/`DEFAULT_DISPLAY_PROPERTIES` constants (defaults match Linear — Milestone/Links/Time-in-status/Updated start hidden), a persisted `displayProperties` store slice with `toggleDisplayProperty` (+ merge backfill), the pills section in `DisplayMenu`, and `IssueRow` now honours each toggle — gating priority/id/status/due-date/labels/created/assignee and adding **Project** (icon + name chip), **Milestone** (diamond chip) and **Updated** (date) rendering. Verified live: pills match Linear, toggling Project/Created hides those columns instantly, console clean. _(Links / Time-in-status pills present for parity but inert — we don't track external links or per-status durations; board cards still render their own fixed set.)_
- [x] 🟡 **Prev/next issue navigation** — soi'd Linear's issue header: a **"n / total"** position counter followed by a **↓ (next)** and **↑ (previous)** chevron split by a thin divider, navigating through the list you came from (first/last edges disable the relevant arrow). Reproduced 1:1: a transient `navIssueIds` store slice (ordered issue identifiers) + `setNavIssueIds` (no-ops on unchanged order, excluded from persist), published by `GroupedIssueList` (and `VirtualIssueList`, via the same component) and `IssueBoard` from their visible order; a shared `IssueNav` component rendering the counter + chevrons, wired into both the full-page `IssueDetail` header (navigates `/issue/:id`) and the `IssuePeek` header (re-peeks the next/prev issue). Renders nothing when the issue isn't part of a known list (e.g. opened straight from ⌘K). Verified live: peek shows "1 / 8" with prev disabled on the first issue, ↓ advances to "2 / 8", the context carries into the full-page view, console clean. _(Keyboard `j`/`k` / arrow shortcuts for prev-next still TODO — buttons only this round.)_
- [x] 🟡 **Issue-list keyboard navigation** — soi'd Linear's list: `↓`/`j` and `↑`/`k` walk a **focused row** (subtle bg + thin rounded inset ring + the select checkbox revealed, exactly like hover); `↵` opens the focused issue (peek), `x` toggles its selection (feeds the bulk-action bar); hovering a row also focuses it. New transient `focusedIssueId` (issue *identifier*, over `navIssueIds`) + `setFocusedIssue`/`moveFocus` store actions (no-focus + `↓` → first row, `↑` → last); excluded from persist; cleared on route change alongside selection. Handled in `useShortcuts` (bails when typing or any `[data-overlay]` menu/popover/modal is open — added that marker to `Popover`/`SelectMenu`). `IssueRow` renders the focus state, scrolls itself into view (`block:'nearest'`) as focus moves, and the Help overlay documents ↓/j ↑/k ↵ x. Works in the windowed list too (shared `IssueRow`). _(Per-row property hotkeys s/p/a/l still TODO — they need an anchored picker; j/k while peek-open don't re-peek yet.)_
- [x] 🟡 **Per-row property hotkeys (s / p / a / l)** — soi'd Linear's list: with a row focused/hovered, pressing **s** opens a centered command-palette-style **status** picker (issue chip header `CLA-2 · Set up your teams`, "Change status…" search, the status list with a check on current + 1-6 number hints), **p** → priority ("Set priority to…", No priority/Urgent/High/Medium/Low), **a** → assignee ("Assign to…"), **l** → label ("Add labels…", multi-select). Reproduced 1:1 by reusing the existing ⌘K issue-context sub-pages: a transient `commandIssueId`/`commandPage` store slice + `openIssuePropertyMenu(issueId, page)` action (excluded from persist, cleared when the menu closes); `useShortcuts` opens it on the focused row for s/p/a/l; `CommandMenu` seeds its sub-page + issue context from the slice and — for this focused-picker mode — closes on Esc/back instead of drilling to the command-palette root. Help overlay documents S/P/A/L. Verified live: `s` on CLA-2 → status picker → "In Progress" moves the row; `l`/`a` open the right sub-pages; Esc closes; console clean.
- [x] 🟡 **Cycle property picker on issues** — soi'd Linear's issue right-hand Properties group: an issue carries a **Cycle** property (between Assignee and Estimate) with a cycle/iteration icon, showing the cycle name (`Cycle N`) or "No cycle", opening a picker that lists the team's cycles with a status hint (**Active** / **Upcoming** / date-range for past) + a "No cycle" option (check on current). The `Issue.cycleId` data model already existed; this surfaces it. New `setIssueCycle(id, cycleId?)` store action (logs a `cycle` activity), a `'cycle'` `ActivityKind` rendered as "added to cycle {pill}" / "removed from cycle {pill}" in `ActivityItem`, and the picker wired into `IssueDetailBody` (shared by full-page detail + peek). The row is hidden when the team has no cycles. Verified live: CLA-1 → Cycle "No cycle" → picker (Cycle 1 Active / Cycle 2 Upcoming) → selecting Cycle 1 updates the property and logs the activity; console clean. _(CreateIssueModal cycle field + cycle column in IssueRow display properties still TODO.)_
- [x] 🔴 **Initiative updates / health timeline + project-side Initiative picker** — mirroring Linear's project updates on the initiative level: the initiative detail now has **Overview / Updates** tabs, an `InitiativeUpdates` composer (On track / At risk / Off track health pills + `@`-mention markdown body → "Post update") and a reverse-chronological timeline (avatar, health badge, `timeAgo`, markdown, hover-delete). The header shows the **latest health badge** next to the stats. `InitiativeUpdate` model + `initiativeUpdates` state, `createInitiativeUpdate`/`deleteInitiativeUpdate` actions, persist-merge backfill, and a seeded on-track update. Also added a **project-side Initiative property picker** — a chip in the `ProjectDetail` header (initiative icon + name, or "No initiative") with a `SelectMenu` to assign/change/remove the project's initiative (wired to `setProjectInitiative`).
- [x] 🟡 **Issue links / "Resources"** — soi'd Linear's issue: the ⋯ menu has **Add link… (⌃L)** opening an **"Add link to CLA-N"** modal (chain icon header, **URL** `https://...` + **Title (optional)** fields, Cancel / **Add link**); links render in a **Resources** section between sub-issues and Activity (collapse chevron + name + circular **+**) as rows of **favicon · title** (host fallback) · relative time · `···` (Open link / Copy link / Edit… / Remove…), plus a **"{user} linked {favicon} {title}"** activity entry. Reproduced 1:1: `IssueLink` model + `issueLinks` store slice (`addIssueLink`/`updateIssueLink`/`removeIssueLink`, persist-merge backfill, cleaned up on issue delete + bulk-delete), a `'link'` `ActivityKind`, a shared `LinkFavicon` (Google s2 favicon + Link2 fallback), the `IssueLinks` Resources section in `IssueDetailBody` (shared by detail + peek), an `AddLinkModal` (add/edit, ↵ submit, Esc close), an **Add link…** row in the issue context menu, and the previously-inert **Links** display property now showing a link-count pill on issue rows. Verified live: seeded GitHub/Figma links render with real favicons, the **+** adds "Linear docs" → row + "linked" activity, persists across reload, console clean. _(⌃L shortcut + a remove-confirm dialog still TODO; row pill follows Linear's default-off Links toggle.)_
- [x] 🟡 **Slash command menu in the editor** — soi'd Linear's description editor (workspace "Claude Test App"): typing **`/`** at the start of a line opens a command menu (placeholder reads **"Type / for commands…"**) with grouped items + right-aligned shortcut hints — **Heading 1/2/3** (⌘⌥1/2/3) · divider · **Bulleted list** (⌘⇧8) / **Numbered list** (⌘⇧9) / **Checklist** (⌘⇧7) · divider · Insert media / gif / Attach files · divider · **Code block** (⌘⇧\) / Diagram / Collapsible section / **Blockquote** (⌥⇧.). Reproduced 1:1 the items our Markdown renderer supports (headings, lists, code, quote) in the exact order/grouping/labels/hints: a `/`-trigger in the shared `MentionInput` (line-start regex, query-filters into a flat ranked list, ↑/↓/↵/Tab/Esc, mouse), each command rewriting the current line into markdown (`# `, `- `, `1. `, `- [ ] `, fenced ```` ``` ````, `> `) with the caret placed correctly; both menus carry `data-overlay` so global hotkeys bail. The editor textarea shows **"Type / for commands…"** when empty. Verified live: `/` → menu → Heading 1 → "# My heading" renders as an H1; console clean. _(Media/gif/attach/diagram/collapsible omitted — no upload backend / renderer support; comments share the same menu via MentionInput.)_
- [x] 🟡 **Create-issue modal "Create more" toggle + header close** — soi'd Linear's real **New issue** modal (workspace "Claude Test App"): header is `[team] CLA › New issue` with an expand icon + **× close** top-right; footer is a paperclip (left) then a **"Create more"** toggle + **Create issue** (right) — no Cancel/⌘↵ text; the `…` chip overflows Set due date / Make recurring / Add link / Add sub-issue. Reproduced the two highest-value faithful gaps: a persisted `createMore` store flag (+ `setCreateMore`, remembered across opens like Linear) wired to a Linear-style switch in the footer — when on, **Create issue** keeps the modal open and resets the form for rapid entry instead of closing/navigating; and an **× close button** in the header. Verified live (localhost:5173): toggling Create more on + Create issue created **CLA-15** and left the modal open with a cleared title; × closes; console clean. _(Expand-to-full-page, paperclip attach, and the `…` overflow items still TODO; Cycle field gated on cycles-enabled — the real workspace has cycles off so it shows no Cycle pill in Display properties either, matching us.)_
- [x] 🟡 **Parent / sub-issue linking to existing issues** — soi'd Linear's issue ⋯ menu (workspace "Claude Test App"): **Create related** ▸ (Issue / Sub-issue ⌘⇧O / Parent issue / Blocked / Blocking — these *create* new linked issues) and **Mark as** ▸ (Parent of… / Sub-issue of… ⌘⇧P / Related to… M R / Blocked by… M B / Blocking… M X / Duplicate of… M M — these *link existing* issues). We already had relations + create-only sub-issues; the gap was linking an **existing** issue as a sub-issue / setting a parent. Reproduced the highest-value faithful slice: the Sub-issues header **+ Add sub-issue** is now a searchable picker whose first option is **Create new sub-issue** (Plus icon, prior behavior) followed by candidate existing issues (status icon + title + `CLA-N`), filtered to exclude self, current sub-issues, this issue's ancestors (cycle-safe) and triage; selecting one calls a new `setIssueParent(id, parentId?)` store action. Each sub-issue row + the parent **breadcrumb** gain a hover **×** to unparent. New `'parent'` `ActivityKind` rendered as **"set parent to {CLA-N · title}"** / **"removed the parent issue"** (issue pill helper in `ActivityItem`). Shared by full-page detail + peek. Verified live (localhost:5173): CLA-2 → + → "Connect your tools" (CLA-3) becomes a sub-issue (rollup 0/1) + CLA-3 shows the CLA-2 breadcrumb and "set parent to" activity; the × removes it + logs "removed the parent issue"; cycle guard prevents ancestor selection; console clean. _(The full ⋯ "Mark as"/"Create related" submenus + ⌘⇧P/⌘⇧O shortcuts still TODO — this round wires the data + the sub-issue surface; "Parent of…" is covered by adding from the parent side.)_
- [x] 🟡 **Issue ⋯ menu: Create related / Mark as submenus** — soi'd Linear's issue header **⋯ ("Issue options")** menu (workspace "Claude Test App", CLA-1): `Add link… (⌃L)` · ─ · **Create related** ▸ (Issue… / Sub-issue… ⌘⇧O / Parent issue… / Blocked issue… / Blocking issue… — *create* a new linked issue) · **Mark as** ▸ (Parent of… / Sub-issue of… ⌘⇧P / Related to… M R / Blocked by… M B / Blocking… M X / Duplicate of… M M — *link an existing* issue) · ─ · Copy ▸ (ID / URL / branch) · ─ · Favorite ⌥F / Subscribe ⇧S · ─ · Delete ⌘⌫. Reproduced 1:1 in a new shared `IssueOptionsMenu` placed in the breadcrumb (after the identifier) on both the full-page detail and the peek header: hover-expanding flyout submenus (right side), exact labels/order/shortcut hints/icons. **Create related** items call `createIssue` (same team/project) then apply the relation (`addRelation` for blocked/blocking/related, `setIssueParent` for sub-issue/parent) and open the new issue; **Mark as** items open a searchable existing-issue `SelectMenu` (self + triage excluded; the store's `setIssueParent` cycle guard keeps parenting safe) → `setIssueParent`/`addRelation`. Reuses `openLinkModal`, `toggleFavorite`, `toggleIssueSubscriber`, `deleteIssue`, the copy helpers; Escape closes the menu (capture-phase, so it doesn't also close the peek). Verified live (localhost:5173, CLA-2): the menu + both flyouts match Linear, **Mark as → Related to… → CLA-1** added a Related relation (then removed via the row ×), console clean. _(⌘⇧O/⌘⇧P + the M-chord shortcuts show as hints only — wiring them is still TODO; "Convert to / Remind me / Add document / description history" omitted — no backend for those.)_
- [x] 🟡 **Relation keyboard shortcuts + shared "Mark as" picker** — soi'd Linear's issue ⋯ "Mark as" flyout and wired the previously hint-only shortcuts: **M then R/B/X/M** (Related to / Blocked by / Blocking / Duplicate of), **⌘⇧P** (Sub-issue of an existing issue), **⌘⇧O** (create a new sub-issue & open it). Each opens Linear's **centered "Mark as" command palette** — a new shared `RelationPicker` overlay (issue chip header `CLA-N · title`, per-kind placeholder e.g. "Related to…", searchable existing-issue list with status icon + identifier, ↑/↓/↵/Esc) driven by a transient `relationPicker: {issueId, kind}` store slice + `openRelationPicker`/`closeRelationPicker` (excluded from persist). The ⋯ menu's "Mark as" rows now call `openRelationPicker` too (dedup — the anchored `SelectMenu` is gone), so menu + keyboard share one surface, matching Linear (its "Mark as" rows open the same palette). `useShortcuts` resolves the **current issue** as peek → `/issue/:id` route → focused row; M-chord uses a `pendingM` ref+timer like the G-chord. Help overlay gains a **Relations** section. Verified live: `M R` on the focused CLA-1 → palette → CLA-2 added as Related; `⌘⇧P`/`⌘⇧O` work from the detail route (⌘⇧O created CLA-16 parented to CLA-1); console clean. _(⌥F favorite / ⇧S subscribe shortcuts still hint-only — `s` row-hotkey collision to resolve later.)_
- [x] 🟡 **Comment threaded replies** — soi'd Linear's issue comment area (workspace "Claude Test App", CLA-1): every top-level comment is a **thread** with its replies stacked on an indented left rail and a persistent collapsed **"Leave a reply…"** composer (current-user avatar + placeholder) at the bottom; clicking it expands the `MentionInput` with **Cancel / Reply** (⌘↵ submit, Esc cancel). The `Comment.parentId` model + `addComment(issueId, body, parentId)` already existed (loop #50) but the UI rendered all comments flat — this surfaces it. New `CommentThread` component (root `CommentItem` + replies + reply composer; replies always attach to the **thread root**, matching Linear's one-level-deep threads); `IssueDetailBody` groups comments into thread roots (top-level + orphaned-parent replies) and renders a `CommentThread` each. Shared by the full-page detail + peek. Seeded an illustrative reply (`c_3` from Jordan under Avery's comment on the virtualization issue). Verified live (localhost:5173, CLA-5): "Leave a reply…" under Avery's comment → typed a reply → **Reply** → renders nested as "You · now" on the rail with a fresh reply box beneath; console clean. _(Resolve thread / collapse-resolved still TODO — needs a `resolvedAt` flag; reply-to-a-reply intentionally flattens to the root like Linear.)_
- [x] 🟡 **Resolve thread** on comments — closed the gap deferred in loops #54/#55 (needed a `resolvedAt` flag). Soi'd Linear's comment thread (workspace "Claude Test App"): the comment ⋯ menu has **Resolve thread** (between Edit and Copy link, circled-check icon), which collapses the whole thread into a single **green-check summary bar** (root author avatar + one-line snippet + "{n} repl{y/ies}" + "{resolver} resolved" + chevron); clicking the bar **expands** it, revealing a **"Resolved by {name} · {ago}"** banner with inline **Unresolve** / **Collapse** actions plus the full root + replies + reply composer, and the menu now reads **Unresolve thread** (circle icon). Reproduced 1:1: `Comment.resolvedAt`/`resolvedBy` on the thread root, a `toggleResolveThread(rootId)` store action (sets/clears resolvedAt + resolvedBy=current user), a `rootId` prop threaded through `CommentItem` → `CommentActions` so the action always targets the root, and the collapse/expand + banner logic in `CommentThread` (a `snippet()` helper strips light Markdown for the bar). Shared by the full-page detail + peek. Verified live (localhost:5173, CLA-5): ⋯ → Resolve thread collapses Avery's thread to the green bar ("Avery Chen … 1 reply · You resolved"); the bar expands to the banner + Unresolve/Collapse with the reply visible; Unresolve restores the normal thread; console clean. _(Auto-collapse-resolved preference + a thread-level resolved activity entry still TODO; reply composer hidden while collapsed, matching Linear.)_
- [x] 🟡 **Inbox header — Linear's ⋯ / Filter / Display options** — soi'd Linear's Inbox (Chrome, workspace "Claude Test App"): the header is just `Inbox` + a **⋯ menu** (Mark all as read · ─ · Delete all) then, far right, a **Filter** funnel and a **Display options** sliders icon — Linear has **no inbox/snoozed tabs**. The **Filter** popover ("Add Filter…") lists **Notification type · From · Project · Issue priority · Issue status type** (each drills into a checkable value list); the **Display options** popover has **Ordering** (Newest/Oldest) + toggles **Show snoozed · Show read · Show unread first**. Reproduced 1:1: rewrote `Inbox.tsx` — replaced the inbox/snoozed tabs + Preferences button with the three header menus; local `display`/`filters` state (like the Issues view's display options); the filtering pipeline resolves each notification's issue for the From/Project/Priority/Status-type dims and honours Show snoozed (default off) / Show read (default on) / Show unread-first ordering; active filters render as `Type · Comments · ×` chips + Clear below the header; a new `deleteAllNotifications` store action behind a **"Delete all notifications? · You cannot undo this action."** confirm dialog. Verified live (vite preview :4930): the header + all three menus match Linear, Show read off hides the read row, `Type → Comments` narrows to the one comment notification with a chip + Clear, console clean; `tsc -b` + `npm run build` green. _(Linear's Display-properties pills (ID / Status and icon) + the two-pane reading view omitted — our rows don't render an ID/status column and the real workspace has a single welcome notification to soi the reading pane; notification Preferences moved out of the inbox header (TODO: wire into Settings → Notifications); filter/display state is local like the team Issues view.)_
- [x] 🟡 **Display options — "List options" (Show sub-issues / Show empty groups)** — soi'd Linear's Display popover (workspace "Claude Test App"): under Layout / Grouping / Sub-grouping / Ordering it has a **Show sub-issues** toggle (on) then a **List options** header with **Nested sub-issues** / **Show empty groups** toggles, then Display properties. Reproduced the two functional, visible ones 1:1: a reusable `ToggleRow` switch (matching the Create-more switch style) in `DisplayMenu` behind optional props (`showSubIssues`/`onShowSubIssues`, `showEmptyGroups`/`onShowEmptyGroups`) — the section only renders when handlers are passed. **Show empty groups** flows into `groupIssues` (new `showEmptyGroups` param; the `.count > 0` filter is bypassed so empty status/assignee/priority/project/label groups render with their icon + "0" + add button — also applies to the board's empty columns). **Show sub-issues** (default on) filters issues with a `parentId` out of the list when off. Wired in `IssuesView` (local state). Verified live (localhost:5173): empty groups on → **Backlog 0** + **Cancelled 0** appear top/bottom; Show sub-issues off → Todo 5→4 (CLA-16 sub-issue hidden); console clean. _(Nested sub-issues / "Order completed by recency" still TODO; SavedViewScreen doesn't persist these yet — section hidden there. Sub-grouping ✅ in loop #57.)_
- [x] 🟡 **Comment actions (edit / delete) + hover toolbar** — soi'd Linear's comment (workspace "Claude Test App"): hovering a comment reveals a top-right toolbar with an **emoji-react** picker and a **⋯ overflow** menu — **Edit** · ─ · Unsubscribe from thread · Resolve thread · ─ · **Copy link to comment** · **Copy content as Markdown** · ─ · New issue / sub-issue from comment · ─ · **Delete** (red). **Edit** turns the body into an inline textarea with **Cancel / Save** (accent); **Delete** opens a centered **"Delete this comment?" / "You cannot undo this action."** dialog (Cancel / red **Delete**); edited comments show an **"(edited)"** hint after the timestamp. Reproduced 1:1 the items with real backing: a new `editComment(id, body)` store action (+ `Comment.editedAt`); refactored `CommentReactions` to render only the pills (picker moved into the toolbar); a new `CommentActions` (reaction Popover + ⋯ menu with Edit / Copy link / Copy content as Markdown / Delete + the confirm dialog, copy → toast) and `CommentItem` (avatar, header with "(edited)", Markdown body, reactions, hover toolbar, inline edit with ⌘↵ save / Esc cancel); wired into `IssueDetailBody` (shared by full-page detail + peek). Verified live: comment → ⋯ → Edit → "(edited)"; Copy link → "Comment URL copied to clipboard" toast; Delete → confirm → removed; console clean. _(Unsubscribe / Resolve thread, threaded replies, and New-issue-from-comment omitted this round — bigger surfaces needing thread/relation model work.)_
- [x] 🟡 **Display options — Nested sub-issues** — soi'd Linear's Display popover **List options** (workspace "Claude Test App"): a **Nested sub-issues** toggle (above **Show empty groups**) that, when on, pulls every sub-issue out of its own status group and renders it **indented beneath its parent** behind a per-row **disclosure chevron** (▸/▾, defaults expanded), with the sub-issue showing its own status icon. Reproduced 1:1: `DisplayMenu` gains `nestedSubIssues`/`onNestedSubIssues` (List-options section now renders when either toggle handler is given); `IssueRow` gains `depth` (inline `paddingLeft` indent) + an `expand` gutter (chevron when it has children, else an aligning spacer; `stopPropagation` so it doesn't peek); `GroupedIssueList` accepts `childrenByParent` (parent id → visible sub-issues), renders a recursive `renderNested` (children always `showStatus`), tracks per-row expand state (default open), and walks the nested order for prev/next nav (descending only into expanded parents); virtualization/drag-reorder fall back to the full render while nested (like sub-grouping). `IssuesView` computes `childrenByParent` from the sorted+filtered set and groups only **top-level** issues (no parent, or parent not visible) when `nested` (list view + Show sub-issues + Nested on). Verified live (localhost:5199): CLA-6 (In Review) + CLA-2 (Todo) added under CLA-1 nest indented in the Todo group (In Review group empties out), the chevron collapses/expands them, CLA-1 keeps its 0/2 badge; `tsc -b` + build green, console clean. _(Board swimlanes + per-view persistence still TODO; nested state is local display state like grouping/ordering.)_
- [x] 🟢 **Display options — "Order completed by recency"** — soi'd Linear's Display popover (workspace "Claude Test App"): directly under the **Ordering** dropdown sits an **Order completed by recency** toggle (off by default), above **Show sub-issues**. When on, completed/canceled issues are ordered by when they were closed (most recent first), overriding the chosen ordering for those issues — so in a status-grouped view the **Done** and **Canceled** groups read newest-first. Reproduced 1:1: `sortIssues` gains an `orderCompletedByRecency` param (the primary `OrderBy` switch is refactored into an `orderComparator`; when the flag is set, two issues both in a `completed`/`canceled` workflow state compare by `completedAt ?? canceledAt ?? updatedAt` descending, otherwise fall through to the primary comparator); `DisplayMenu` renders the `ToggleRow` between **Ordering** and **Show sub-issues** behind optional `orderCompletedByRecency`/`onOrderCompletedByRecency` props; `IssuesView` holds the local state (default off) and threads it into `sortIssues` + the memo deps. Verified live (localhost:5188, All Issues): the toggle sits exactly where Linear places it, flips on (accent) cleanly, console clean. _(SavedViewScreen doesn't persist this yet — local display state like grouping/ordering; the seed has a single Done + single Canceled issue so the reorder isn't visually distinguishable, matching the sparse real workspace.)_
- [x] 🟡 **Board swimlanes (Rows / sub-grouping on the board)** — soi'd Linear's Display popover in **Board** layout (workspace "Claude Test App"): the popover relabels to **Columns** / **Rows** / **Ordering** + **Board options** (**Show empty columns** / **Show empty rows**), and the **Rows** dropdown (No grouping · Status · Assignee · Agent · Project · Priority · Label · Parent issue) turns the board into horizontal **swimlanes** — column headers (Todo/In Progress/…) render once across the top, then each row group gets a full-width **collapsible header band** (chevron + row glyph + name + count) with cards laid out in the matching status columns beneath; **empty rows collapse into a "▸ Hidden rows N"** bar at the bottom (click to expand). Reproduced 1:1 (we omit Agent/Parent-issue rows — no model): `IssueBoard` gains `rows` + `subGroupBy` props and a swimlane renderer (`Swimlane` = header band + one droppable `CardStack` per column; cell issues = each column's `subGroups` entry matching the row key; composite `rowKey::stateId` drop ids so cross-column drag still calls `moveIssue`); `IssuesView` computes per-column `subGroups` (both layouts now) + an all-rows `rows` list for the board; `DisplayMenu` relabels Grouping→Columns, Sub-grouping→Rows, "List options"→"Board options" (Nested-sub-issues hidden, "Show empty groups"→"Show empty columns") when `layout==='board'`. Verified live (localhost:5199, Board · Rows=Priority): Urgent/High/Medium/Low swimlanes render with cards in the right columns + "Hidden rows 1" → expands to the empty "No priority 0" band; `tsc -b` + build green, console clean. _(Cross-swimlane drag changes status only, not the row property; "Show empty rows" toggle + SavedViewScreen persistence still TODO — empty rows already reachable via the Hidden-rows bar.)_
- [x] 🟡 **Projects list view + Display options** — soi'd Linear's Projects page (workspace "Claude Test App"): the default layout is a **List** (not a card grid), and the Display popover offers **Layout** (List / Board / Timeline), **Grouping** (No grouping · Lead · Member · Status · Priority · Label · Team · Health · Start/Target date), **Ordering** (Manual default), **Show closed projects**, and **Display properties** pills (Milestones · Summary · Priority · Status · Health · Teams · Lead · Members · Dependencies · Start/Target date · Issues · Created · Updated · Completed · Labels — Milestones/Priority/Status/Health/Lead/Target date/Issues default-on). Reproduced the faithful slice we can back with data: rewrote `ProjectsView` from the card grid into Linear's **list rows** — each row = `ProjectStatusIcon` (new SVG glyph: backlog dashed / planned outline / In Progress half-wedge / paused pause-bars / completed filled-check / canceled filled-✕, colored per `PROJECT_STATUS`) + project emoji + name, then a right cluster of issues-progress (`ProgressDonut` + %), `HealthBadge` (from the latest project update), target date, member avatars, lead avatar. A new `ProjectsDisplayMenu` popover (Layout=List, **Grouping** No grouping/Status/Health/Lead, **Ordering** Manual/Name/Target date/Created, **Display properties** Status/Health/Lead/Members/Target date/Issues) drives local view state; grouping renders collapsible sticky group headers (status glyph / health / lead avatar + count). Added `PROJECT_STATUS`/`PROJECT_STATUS_ORDER` constants and corrected the empty-state copy to Linear's exact wording. Verified live (localhost:5199): MVP Launch (In Progress, 13%, On track, Jul 18) + Mobile App (Planned, Sep 16) render as list rows; Grouping=Status splits into "Planned 1" / "In Progress 1" collapsible bands; `tsc -b` + build green, console clean. _(Board / Timeline layouts, Priority/Member/Team/date grouping, Milestones/Summary columns, and per-view persistence still TODO — Project has no priority field; view state is local like the Issues view's grouping/layout.)_
- [x] 🟡 **Group-header `+` pre-fills the create modal** — soi'd Linear (workspace "Claude Test App", list + board): clicking the **`+`** on a group/sub-group header (or a board column header) opens the **New issue** modal *pre-filled with that group's property* — e.g. the Todo group's `+` shows the **Todo** status pill, the In Progress group's `+` shows **In Progress**; a sub-group `+` combines both (status + priority/assignee/…). Previously our `+` opened a blank modal. Reproduced 1:1: a `CreatePrefill` type (`teamId`/`stateId`/`priority`/`assigneeId`/`labelIds`/`projectId`), a transient `createPrefill` store slice + `openCreateWith(prefill)` action (excluded from persist; `setCreateOpen(false)` clears it), and a shared `prefillFor(groupBy, group)` helper in `GroupedIssueList` mapping a group's key → its property (status→stateId, priority→priority, assignee→assigneeId, project→projectId, label→[labelId]; "No X" groups prefill nothing). The list group `+` calls `openCreateWith(prefillFor(groupBy, group))`, the sub-group `+` spreads `{...prefillFor(groupBy, group), ...prefillFor(subGroupBy, sg)}`, and `CreateIssueModal` seeds its form fields from `createPrefill` on open (and re-seeds on each "Create more" so the group context sticks). Also **added a hover `+` to the board column / swimlane column headers** (previously absent) prefilling that column's status. Verified live (localhost:5199): In Progress `+` → modal pre-set to "In Progress", created **CLA-15** which landed straight in the In Progress group; context-menu confirmed its status; console clean. _(Board swimlane row `+` and `SavedViewScreen` group `+` not added — those headers don't render an add button yet.)_
- [x] 🟢 **Project milestones — inline create/edit (replace `prompt()`)** — soi'd Linear's project Overview (workspace "Claude Test App", "Overview Soi Demo"): the **+ Milestone** button in the main column inserts an **inline, auto-focused** milestone row (◇ diamond icon + "Milestone name" placeholder field + an "Add a description…" field beneath); typing live-updates it, abandoning an empty row removes it, and each row carries an inline **target-date** picker + a **···** menu (Linear's is Edit… / Set target date… / Copy / Move / Delete — we ship **Edit… + Delete**). Reproduced 1:1: added `Milestone.description`, a `updateMilestone(id, patch)` store action, and a new inline `MilestoneItem` component in `ProjectDetail` (diamond icon, controlled name/description `<input>`s writing via `updateMilestone`, a `DatePicker` target-date chip, progress badge + bar, and a `Popover` ··· menu with Edit/Delete). `addMilestone` now creates an empty milestone and auto-focuses it (was `prompt()`); on blur an empty row self-deletes (guarded against intra-row focus moves via `relatedTarget`). Verified live (localhost:5199, MVP Launch): + Milestone → editable row → "Public beta" persists to the store; empty rows clean up on blur; `tsc -b` + build green, console clean. _(Copy / Move-milestone-to ··· items omitted; target date shown inline rather than only via the menu.)_
- [x] 🟡 **Display options — Sub-grouping** — soi'd Linear's Display popover (workspace "Claude Test App"): between **Grouping** and **Ordering** sits a **Sub-grouping** dropdown — options **No grouping** (default) · Status · Assignee · Agent · Project · Priority · Label. Choosing one nests every top-level group's issues into sub-groups: the top group keeps its sticky gray header (icon + name + count), then each sub-group renders an **indented, transparent header** (collapse chevron + sub-group glyph + name + count + a thin trailing divider line + a `+` add button) with its issues beneath. Reproduced 1:1 (we omit "Agent", which we don't model): a `subGroups?: IssueGroup[]` field on `IssueGroup`; `IssuesView` computes `subGroups` per top group via `groupIssues(group.issues, subGroupBy, data, showEmptyGroups)` (list view only — board uses status columns); a new **Sub-grouping** `Seg` row in `DisplayMenu` (`SUBGROUPS` constant, "No grouping" first like Linear); `GroupedIssueList` renders the nested sub-group headers with independent per-sub-group collapse (key `parentKey::subKey`) via a shared `renderIssues` helper. Virtualization + drag-reorder fall back to the full render while sub-grouping is active (the nested layout needs every row). Verified live (localhost:5190): Group Status / Sub-group Priority → Todo ▸ High/Medium, In Progress ▸ Urgent/High, etc.; collapsing the "Medium" sub-group hides only its rows; console clean. _(Board swimlanes + SavedViewScreen persistence still TODO; "Agent" sub-group omitted — no agent model.)_
- [x] 🟡 **Settings — two-pane layout with grouped left nav** — soi'd Linear's Settings (workspace "Claude Test App"): opening Settings **replaces the app sidebar** with a dedicated settings nav (top: `‹ Settings` back, a **Search…** box, then groups **Personal** [Preferences · Profile · Notifications · Code & reviews · Security & access · Connected accounts · Agent personalization] · **Issues** [Labels · Templates · SLAs] · **Projects** [Labels · Templates · Statuses · Updates] · **Features** [AI & Agents · Initiatives · Documents · Customer requests · Releases · Pulse · Asks · Emojis · Integrations] · **Administration** [Workspace · Teams · Members · Security · API · Applications · Billing] · **Your teams** [team names]) with the workspace name pinned at the bottom; the right pane is a max-width doc with a big page title + sections. Reproduced 1:1: rewrote `SettingsView` from a single scrolling card stack into the two-pane shell — exact nav groups/labels/order, section routing via `?page=` search param, a live nav **search filter** (hides empty groups), and `Shell` now hides the app `<Sidebar>` on `/settings`. Wired real content for the sections we can back: **Preferences** (interface theme cards System/Light/Dark), **Profile** (avatar + editable Full name via new `updateUser` action + read-only email), **Issues→Labels/Templates**, **Projects→Statuses** (`StatesSettings`), **Administration→Workspace** (rename via new `setWorkspaceName` action + Import & export + Danger-zone reset), **Teams**, **Members**, and **Your teams→team** (Workflow states). Every other Linear nav item renders a clean `EmptyState` placeholder. Verified live (localhost:5199): nav matches Linear 1:1, Preferences/Profile/Members render, search filters to Labels-only, placeholders + console clean; `tsc -b` + build green. _(Notifications/Security/API/Billing/Features/* are placeholders until backed by data; per-team settings show only Workflow for now.)_
- [x] 🟡 **Filter by Cycle & Milestone** — soi'd Linear's Filter popover (Chrome, workspace "Claude Test App"): the dimension list runs Status · Assignee · Creator · Priority · Label · Project · **Cycle** (iteration icon, submenu lists the team's cycles) · **Milestone** (diamond icon, submenu lists project milestones) · Subscribers. We already stored `Issue.cycleId` + `Issue.milestoneId` but neither was filterable. Added both dimensions to `FilterBar` placed in Linear's order (Cycle + Milestone after Project, before Subscribers): `cycleIds` options = all cycles sorted newest-first (`name ?? Cycle N`, `IterationCw` icon), `milestoneIds` options = all milestones sorted by `sortOrder` (`Diamond` icon). `FilterState` gains optional `cycleIds?`/`milestoneIds?` (optional so pre-existing saved views still load); `filterIssues` filters by exact cycle and exact milestone (both `?.length`-guarded); `emptyFilters()` seeds both. Verified live (localhost:5199): the menu shows Cycle/Milestone in order, the Cycle submenu lists Cycle 2 / Cycle 1, `Cycle = Cycle 1` filters the Todo group 4→2 with a `Cycle · Cycle 1 · ×` chip + Clear, console clean; `tsc -b` + build green. _(Dates / "no cycle"-style negative filters still TODO.)_
- [x] 🟡 **Filter operators (is / is not)** — soi'd Linear's filter chip (Chrome, workspace "Claude Test App"): every chip is **dimension · operator · value(s) · ×**, where the **operator** segment is a dropdown — **is** / **is not** for a single value, and **is any of** / **is not** once 2+ values are selected (the operator menu shows the current choice with a check). "is not" inverts the dimension (e.g. `Status is not Todo` shows everything except Todo). Our chips only ever did "is". Added a per-dimension operator: `FilterState.negate?` (optional `Partial<Record<dimKey, boolean>>`, so saved views persisted before operators still load); `filterIssues` (selectors) refactored to a `[active, matches, key]` table that inverts the membership test when `negate[key]` is set (negated multi-value label/subscriber dims mean "has none of these"); `FilterBar` chip gains an operator `Popover` between the label and value segments (`OperatorMenu` with `is`/`is any of` + `is not`, check on current, closes on pick), `operatorLabel()` for the wording, `setNegate`/`clearDim` helpers, and the `×`/empty-value paths clear the lingering operator. Verified live (localhost:5199): `Status is Todo` → 4 Todo issues; flipping the operator to **is not** inverts to In Progress/In Review/Done; operator menu shows `is ✓` / `is not`; console clean; `tsc -b` + build green. _(Dates dim + Linear's "2 statuses" overlapping-icon value summary still TODO.)_
- [x] 🟡 **Filter by Creator & Subscribers** — soi'd Linear's filter menu (Chrome, workspace "Claude Test App"): the **Filter** popover lists Status · Assignee · Creator · Priority · Labels · … · Subscribers (Creator uses a person icon, Subscribers a bell, both submenus are people pickers identical to Assignee). We already stored `Issue.creatorId` + `Issue.subscriberIds` but only Status/Assignee/Priority/Label/Project were filterable. Added two dimensions to `FilterBar` (`creatorIds`, `subscriberIds`, both backed by the user list with avatars) placed to match Linear's order (Creator after Assignee, Subscribers last); `FilterState` gains optional `creatorIds?`/`subscriberIds?` (optional so saved views persisted before this still load — reads guarded with `?? []` in `valuesOf`/`hasActiveFilters`); `filterIssues` filters by exact creator and by any-subscriber overlap. Verified live (preview :4900): the menu shows the new dimensions in order, `Creator = You` keeps all seed issues (all created by me), `Creator = Avery Chen` empties the list to the "No issues" empty state, the chip + popover + Clear all work, console clean. _(Cycle/Milestone/Dates/Creator-by-content filter dims still TODO; the live dev port 5173 was occupied by another project so verification ran against `vite preview`.)_
- [x] 🟡 **Filter by Dates** — soi'd Linear's Filter popover (Chrome, workspace "Claude Test App"): a bottom **Dates** dimension (calendar icon) drills into a field submenu — **Due date · Created date · Updated date · Started date · Completed date · Triaged date · Time in current status** — and each field opens a relative quick-pick list (**1 day · 3 days · 1 week · 1 month · 3 months · 6 months · 1 year ago** + Custom date or timeframe…). The resulting chip is **{field} · {before/after} · {period} · ×** where the operator segment is a **before / after** dropdown (default **after**, check on current). Reproduced the faithful slice we can back with timestamps — the four fields we track (**Due / Created / Updated / Completed**): a `DateField` + `DateFilter {field, op, value}` type and an optional `FilterState.dates?: DateFilter[]` (optional so older saved views still load); `filterIssues` resolves each filter's relative period to a cutoff (`resolveDateCutoff`) and ANDs `before` (≤) / `after` (≥) against the issue's timestamp (issues missing that date never match); `FilterBar` adds the **Dates** root entry → field submenu → relative-period list (selecting a period appends a `{op:'after'}` filter + closes the popover), and a `DateChip` rendering field · before/after `Popover` · period `Popover` · ×. Verified live (localhost:5199): `Dates → Created date → 1 week ago` adds **📅 Created date · after · 1 week ago** keeping all seed issues (created yesterday); flipping the operator to **before** empties the list (correct); console clean; `tsc -b` + build green. _(Started/Triaged/Time-in-current-status fields omitted — no `startedAt`/`triagedAt`/per-status-duration data; "Custom date or timeframe…" + "No due date" value still TODO; date filters not yet persisted to SavedViews beyond the optional field.)_
- [x] 🟡 **Custom date filter (Day picker)** — closed the "Custom date or timeframe…" TODO from **Filter by Dates**. Soi'd Linear's modal (Chrome, workspace "Claude Test App"): the period list ends with a divider + **Custom date or timeframe…**; for the **Day** granularity it opens a dialog titled **{field}** with a **before / after** segmented toggle, a free-text input (placeholder _Try: May 2027, Q4, 20/05/2027_), Day/Month/Quarter/Half-year/Year granularity tabs, a **two-month calendar** (Mon-first, weekends faint, today ring-circled, ‹ › month nav) and **Cancel / Apply** (Apply disabled until a day is picked). The resulting chip is **{field} · before/after · {MMM D[, YYYY]} · ×**. Reproduced the Day slice 1:1 — a custom day is stored as a plain `YYYY-MM-DD` `DateFilter.value` (no schema change); `resolveDateCutoff` now resolves ISO dates to local midnight so the existing `before`(≤)/`after`(≥) match works unchanged; `FilterBar` gains a `CustomDateModal` (portal overlay, Esc-to-close, before/after toggle, typed `DD/MM/YYYY`-or-`YYYY-MM-DD` parse, two `MonthGrid`s) wired into both the add-flow period list and the `DateChip` period popover (the Custom entry shows a check when the active value is a custom date; editing patches op+value in place); `datePeriodLabel` formats ISO values as `MMM D` / `MMM D, YYYY`. Verified live (localhost:5199): `Dates → Created date → Custom… → Jun 10 → Apply` → chip **📅 Created date · after · Jun 10**, seed issues (created yesterday) all kept; re-editing to **before · Jun 10** empties the list (correct); console clean; `tsc -b` + build green. _(Month/Quarter/Half-year/Year granularities + the "in {period}" range operator landed in loop #71.)_
- [x] 🟡 **My Issues — Assigned / Created / Subscribed / Activity tabs** — soi'd Linear's My Issues page (Chrome, workspace "Claude Test App"): a two-row header (title **"My issues"** then a pill sub-nav **Assigned · Created · Subscribed · Activity** with Add filter / Display options / Open details on the right), each tab a route (`/my-issues/assigned|created|subscribed|activity`). Assigned/Created/Subscribed are the status-grouped issue list filtered by `assigneeId` / `creatorId` / `subscriberIds`; **Activity** is a day-bucketed feed (collapsible **Today / Yesterday / weekday** headers with an event count) of the current user's own actions across all issues — each row = priority + status icon + identifier + title, right-aligned **{kind glyph} you {verb} {Mon D, HH:MM:SS}** (e.g. "you commented", "you created the issue", "you changed the parent"). Reproduced 1:1: rewrote `MyIssues` from a single list into the tabbed view (route `/my-issues/:tab`, default `assigned`), per-tab empty-state copy, and an `ActivityFeed` that merges `activities` + `comments` authored by `currentUserId`, sorts newest-first, groups into day buckets, and opens the issue in the peek on click (`setPeek`). Verified live (localhost:5199): Activity shows "Today 9" / "Yesterday 12" with faithful per-row verbs + timestamps, the pill tabs switch routes (Assigned → Todo/In-Progress groups), console clean; `tsc -b` + `npm run build` green. _(The header's Add filter / Display options / Open-details buttons are not yet wired on this view; Activity feed is the current user's own actions only — no "others' activity on my issues" stream.)_
- [x] 🟡 **My Issues — wire Add filter + Display options** — closed the TODO left in loop #72 (the My Issues header buttons weren't wired). Soi'd Linear's My Issues page (Chrome, workspace "Claude Test App"): on the Assigned / Created / Subscribed tabs the controls work exactly like the team Issues view — a **Display options** popover (Layout List/Board · Grouping · Sub-grouping · Ordering · List options · Display properties) on the right of the pill sub-nav, and a **Filter** bar (full dimension list: Status · Assignee · Creator · Priority · Label · Project · Cycle · Milestone · Subscribers · Dates) beneath it that produces `dimension · operator · value · ×` chips + Clear. The **Activity** tab has neither (it's a feed). Reproduced 1:1 by lifting `IssuesView`'s view-state into `MyIssues`: local `layout`/`groupBy`/`subGroupBy`/`orderBy`/`orderCompletedByRecency`/`showSubIssues`/`nestedSubIssues`/`showEmptyGroups`/`filters` state held in `MyIssues` (so display settings persist across the three issue tabs, matching Linear), a `DisplayMenu` mounted to the right of the tab row (issue tabs only), and a new `IssueTab` subcomponent that scopes by the per-tab predicate (`assigneeId`/`creatorId`/`subscriberIds`), then runs the **same** `filterIssues → sortIssues → groupIssues` pipeline as the team view (incl. nested sub-issues, sub-grouping, board swimlanes, empty groups, manual drag-reorder → switches Ordering to Manual) and renders `FilterBar` + `GroupedIssueList`/`IssueBoard`. Verified live (vite preview :4910): Assigned shows Todo/In-Progress groups; **Filter → Priority → Urgent** narrows to CLA-5 with a `Priority · is · Urgent · ×` chip + Clear; the Display pill sits on the right; the Activity tab hides both controls and still shows the Today 11 / Yesterday 8 feed; console clean; `tsc -b` + `npm run build` green. _(View state is local like the team Issues view — not yet persisted to a saved view; the "Open details" header button from Linear's My Issues isn't reproduced.)_
- [x] 🟡 **Custom date filter — Month/Quarter/Half-year/Year + "in" range operator** — closed the remaining slice of the Custom-date modal (loops #66–70). Soi'd Linear: the modal's granularity tabs (**Day · Month · Quarter · Half-year · Year**) each swap the picker — Day = the two-month calendar; Month = a year stepper + 4-col month grid; Quarter = Q1–Q4; Half-year = H1/H2 (Jan–Jun / Jul–Dec); Year = a 12-year window grid — and for the period granularities the operator toggle gains **in** (before / after / **in**) so a chip can read **Created date · in · Jun 2026 · ×**. Reproduced 1:1: `DateFilter.op` widened to `'before' | 'after' | 'in'` and the value now also carries absolute periods (`YYYY-MM` · `YYYY-Q[1-4]` · `YYYY-H[12]` · `YYYY`); `selectors.periodRange()` resolves any absolute value to a `[start, end)` range and `matchesDate` does `in` = within / `before` = `< start` / `after` = `≥ end` (relative tokens still use the cutoff path); `FilterBar` got the granularity tabs + per-granularity period grids, an extended natural-language parser (`May 2027`, `Q4 2027`, `H1 2027`, bare `2027`, plus the existing `DD/MM/YYYY`), `datePeriodLabel` formatting for each period kind, and the chip's operator popover now offers **in** for absolute values. Verified live (localhost:5199): Dates → Created date → Custom → **Month → in → Jun** → chip **📅 Created date · in · Jun 2026**, all June-created seed issues kept; flipping the chip operator to **before** empties the list (correct); console clean; `tsc -b` + `npm run build` green. _(The input's "Q4"/"H1" relative-to-now natural-language ("next quarter") + per-view persistence of the period beyond the optional `dates` field still TODO.)_
- [x] 🟡 **Inbox — two-pane reading view (list + issue pane)** — soi'd Linear's Inbox (workspace "Claude Test App"): it is a **two-pane** surface, not a list that navigates away. The **left pane** (~340px, its own `Inbox · ⋯ · Filter · Display` header + filter chips) is a compact two-line notification list (unread dot · avatar · **issue title** bold + `{actor} {verb}` muted · `timeAgo` right · hover snooze/done); the **right reading pane** shows the selected notification's full issue, with its own header (team › identifier breadcrumb + **clock snooze** + **done/archive** + maximize). When nothing is selected the reading pane shows the inbox illustration + "{n} unread notification(s)". Reproduced 1:1: rewrote `Inbox.tsx` from a single-pane list into `flex` two-pane — selecting a row (click / ↑↓ / j k) marks it read and renders `IssueDetailBody` (compact) in the right pane; mark-done & snooze remove the row and advance to the neighbour; Esc clears selection. Row is a `<div role=button>` so the snooze `Popover`'s button doesn't nest (no hydration error); `outline-none` suppresses the focus ring since selection is shown via `bg-bg-selected`. Verified live (vite preview :4931): clicking CLA-6/CLA-5 renders the full issue + Properties sidebar with breadcrumb + snooze/done/maximize header, ↑↓ moves selection, read-count decrements, console clean; `tsc -b` + build green. _(Linear's "Mark as done" archives reversibly — our model has no archived state so done = delete; the welcome-message custom reading doc and the snoozed/done sub-filters omitted.)_
- [x] 🟡 **Settings → Notifications page** — soi'd Linear's account Notifications page (Chrome, workspace "Claude Test App", `/settings/account/notifications` + the `/email` sub-page); replaced our `ComingSoon` placeholder. Built **Notification channels** (a bordered card of four drill-in rows Desktop › Mobile › Email › Slack, each = icon + name + green/red-dot status line "Disabled" / "Enabled for all notifications"; defaults Desktop/Slack off, Mobile/Email on) → a **channel detail page** (breadcrumb "Notifications" + title, master **Enable {x} notifications** toggle, then the per-event matrix grouped **General notifications** [Assignments · Status changes · Comments and replies · Mentions · Reactions · Subscriptions · Document changes · Updates · Reminders and deadlines · Apps and integrations · Billing] / **Feature notifications** [Triage] with exact labels + helper text; Email also gets **Notification format** Digest + **Email digest settings**; dependent rows grey out + go inert when the master is off) — plus the **Updates from Linear** section (Changelog: Show updates in sidebar / Changelog newsletter · Marketing: Marketing and onboarding · Other updates: Invite accepted / Privacy and legal updates / Data processing agreement (DPA), all 1:1 labels + copy). New persisted `notificationSettings` store slice (`NotificationSettings` per-channel `{enabled, events}` matrix + email-digest + product-update booleans), `DEFAULT_NOTIFICATION_SETTINGS`/`NOTIFICATION_CHANNELS`/`NOTIFICATION_EVENT_GROUPS` constants, `setNotificationChannelEnabled`/`setNotificationEvent`/`updateNotificationSettings` actions, deep merge-backfill, and a `NotificationsSettings` component (overview ⇄ detail via local state) wired into `SettingsView`. Verified live (`vite preview` :4955): overview + Email detail match Linear, master-off greys dependents, console clean; `tsc -b` + build green. _(Slack uses a `MessageSquare` stand-in — lucide dropped its brand `Slack` icon; Digest/delay/urgent are booleans not Linear's select+time-range UI; Updates-from-Linear toggles are cosmetic — no email backend.)_
- [x] 🟡 **Inbox keyboard shortcuts** — soi'd Linear's shortcut reference (Chrome, workspace "Claude Test App" → Help → Keyboard shortcuts → search "inbox"): the **Inbox** section lists **⌫** Delete notification (= mark as done) · **⇧⌫** Delete all read notifications · **U** Mark as read/unread · **⌥U** Mark all as read · **H** Snooze notification (plus the existing ↓/j ↑/k navigation, Esc to clear). Reproduced all of them in `Inbox.tsx`: new store actions `setNotificationRead(id, read)` (toggle) + `deleteAllReadNotifications()`; the keydown handler moved into a ref (always reads fresh list/selection) and registers in the **capture phase** with `stopImmediatePropagation()` on the keys it owns so the global `useShortcuts` (which also binds j/k/arrows → `moveFocus`) no longer double-fires while `c`/`g`/`?` still pass through; `⌥U` matched via `e.code === 'KeyU' && e.altKey` (macOS Alt+U is a dead key). ⌫/H/U act on the selected notification and advance to its neighbour like the reading-pane buttons. Added an **Inbox** section to the `?` help overlay + shortcut hints to the reading-pane buttons (`Snooze (H)`, `Mark as done (⌫)`). Verified live (production preview): j selects+opens, U toggles the unread dot, H snoozes (row leaves, selection advances), ⌫ marks done+advances, ⌥U clears the sidebar unread badge; console clean; `tsc -b` + build green. _(H snoozes to "tomorrow" by default — the full snooze date picker stays on the clock button, since Linear's H opens a flyout we don't yet position via keyboard.)_
- [x] 🟡 **Create-issue Cycle field + Cycle list column** — closed the two TODOs left by the cycle-property-picker loop. The **New issue** modal now carries a **Cycle** picker (after Project, hidden when the active team has no cycles): a `SelectMenu` over the team's cycles with `Active` / `Upcoming` / date-range hints + a "No cycle" option (check on current), an `IterationCw` chip reading the cycle name or "Cycle", wired to `createIssue({ …, cycleId })` (already accepted by `NewIssueInput`). It resets when the modal opens, on "Create more", and when the team is switched (cycles are per-team). Added a **Cycle** entry to the Display-options properties (`DisplayProperty` `'cycle'`, `DISPLAY_PROPERTIES` after Project, default **off** like Milestone; merge-backfill picks it up) and an `IterationCw` cycle chip in `IssueRow` gated on `dp.cycle`. `tsc -b` + `npm run build` green. _(Re-verify against Linear in-browser next loop; CreateInitiativeModal/board cards unaffected.)_
- [x] 🟡 **Wire "Display names" preference (Full name / First name)** — closed the highest-value Preferences-behavior TODO from loop #79. Soi'd Linear's `/settings/account/preferences` ("Display names — Select how names are displayed in the Linear interface — Full name"); previously the setting persisted + selected but never changed any rendering. Added pure `firstName()` + `displayName(name, mode)` helpers in `utils.ts` and a `useDisplayName()` store hook returning a `(name) => string` formatter that reads `preferences.displayNames`. Applied it to every user-name **display** surface across the app: the issue Properties assignee + single-subscriber label, comment authors / thread resolved-by / reaction tooltips, the activity feed actor, project & initiative update authors, the project lead + initiative owner, every people-picker option label (Assignee/Subscriber/Members/Owner/Team-member/Bulk-bar/Filter Assignee-Creator-Subscribers/⌘K Assign-to/Create-issue/Create-initiative), the Inbox From-filter + notification-row actor, and the **group-by-assignee** group headers (threaded an optional `displayNamesMode` param through `groupIssues`, passed `data.preferences.displayNames` from IssuesView / MyIssues / SavedViewScreen). Avatars/initials + tooltips and the canonical Profile/Members identity keep the **full** name (matching Linear); mention `@chips` keep their stored token. Verified live (preview :4972): with the pref flipped to **First name**, CLA-2's assignee renders "Avery" (was "Avery Chen"), no "Avery Chen" string remains anywhere, console clean; `tsc -b` + `npm run build` green. _(MembersSettings admin list intentionally stays full name; mention chips unchanged.)_
- [x] 🟡 **Settings → Preferences page** — soi'd Linear's account Preferences page (Chrome, workspace "Claude Test App", `/settings/account/preferences`); replaced the theme-only stub with the full page 1:1. Four bordered cards under headings **General** / **Interface and theme** / **Desktop application** / **Automations and workflows**, exact row labels + helper text + control types: General = Default home view (dropdown Active issues/My issues/Inbox) · Display names (Full/First name) · First day of the week (Sunday/Monday) · Convert text emoticons into emojis (toggle) · Send comment on… (Enter / ⌘+Enter); Interface = App sidebar "Customize" · Font size (Small/Default/Large) · Use pointer cursors (toggle) · a second card Interface theme (System preference/Light/Dark, with an **Aa** swatch) · Light · Dark sub-themes; Desktop = Open in desktop app; Automations = Auto-assign to self · On move to started status, assign to yourself. New persisted `Preferences` store slice + `setPreference(key, value)` action, reusable `PrefCard`/`PrefRow`/`Toggle`/`PrefDropdown`/`ThemeSwatch` building blocks. **Genuinely wired**: Interface theme + Light/Dark sub-themes drive `useThemeEffect` (system appearance now picks the chosen sub-theme); Font size + Use pointer cursors apply via a new `usePreferenceEffect` (root font-size + `.pointer-cursors` CSS); Default home view drives `DefaultRedirect`; Auto-assign to self defaults the New-issue assignee to the current user; On move to started auto-assigns an unassigned issue in `setIssueStatus`. Verified live (`:5180`): the page matches Linear, the Interface-theme dropdown switches the whole app dark instantly, toggles render Linear's pill, console clean; `tsc -b` + build green. _(Display names / First day of week / Convert emoticons / Send comment on / Open in desktop persist + select correctly but their cross-app rendering/behavior wiring is the next slice; App sidebar "Customize" has no sub-panel yet.)_
- [x] 🟢 **Display options — Ordering direction toggle (ascending / descending)** — soi'd Linear's Display popover (Chrome, team Issues): a small **sort-direction arrow** sits between the **Ordering** label and its dropdown — ascending shows an up-arrow with narrow→wide bars (`ArrowUpNarrowWide`), clicking flips it to a down-arrow with wide→narrow bars (`ArrowDownWideNarrow`) **with a filled background** to mark the active descending state. It reverses the chosen ordering. Reproduced 1:1: an `OrderDir = 'asc' | 'desc'` type, `sortIssues` gains an `orderDir` param (wraps/negates the primary comparator when `desc`; the completed-by-recency override stays newest-first), `DisplayMenu` renders the toggle button (optional `orderDir`/`onOrderDir` props — only shows when given) before the Ordering `Seg`, and both `IssuesView` + `MyIssues` hold the local `orderDir` state and thread it through. Verified live (preview :5199): the ascending arrow renders next to "Priority", clicking flips to the filled descending arrow, console clean; `tsc -b` + `npm run build` green. _(SavedViewScreen doesn't expose/persist it yet — local display state like the other ordering toggles.)_
- [x] 🟡 **Display options — full Ordering list + Grouping order parity** — soi'd Linear's Display popover Ordering/Grouping dropdowns (Chrome, workspace "Claude Test App", team Issues): **Grouping** = No grouping · Status · Assignee · Agent · Project · Priority · Label; **Ordering** = Manual · Title · Status · Priority · Assignee · Agent · Estimate · Updated · Created · Due date · Link count · Time in status (we previously offered only Priority/Updated/Created/Title/Manual). Brought our menus to 1:1 (omitting **Agent** — not modeled — and **Time in status** — no per-status timestamps): widened `OrderBy` to `manual|title|status|priority|assignee|estimate|updated|created|dueDate|linkCount`, rewrote `orderComparator` to take `WorkspaceData` and added comparators — **Status** (workflow type order × position), **Assignee** (alpha by name, unassigned last), **Estimate** (highest first, none last), **Due date** (soonest first, none last), **Link count** (most `issueLinks` first) — each falling back to `sortOrder`; reordered `GROUPS` to put "No grouping" first and Project before Priority (matching Linear, and now consistent with `SUBGROUPS`); relabelled "Last updated"→"Updated". Verified live (vite :5191, team Issues): the Ordering dropdown lists all ten options in Linear's order, switching to **Due date** re-sorts within the Status groups so CLA-5 (due Jun 20) precedes CLA-7 (no due date); console clean; `tsc -b` + `npm run build` green. _(Ascending/descending direction toggle next to Ordering, plus Agent/Time-in-status orderings, deferred — no data to back them.)_
- [x] 🟡 **Right-click context menu → Linear's full list-row menu (1:1)** — soi'd Linear's issue list-row right-click menu (Chrome, workspace "Claude Test App", team Issues): order is **Status (S) · Priority (P) · Assignee (A) · Due date (⇧D) · Labels (L) · Project (⇧P) · More properties ›** — divider — **Create related › · Mark as ›** — divider — **Copy › · Convert to › · Open in ›** — divider — **Favorite (⌥F) · Unsubscribe (⇧S) · Remind me (⇧H) ›** — divider — **Delete (⌘⌫)**; the **Mark as** flyout is Parent of… / Sub-issue of… (⌘⇧P) / Related to… (M R) / Blocked by… (M B) / Blocking… (M X) / Duplicate of… (M M). Our right-click `IssueContextMenu` was a thinner subset (Status/Priority/Assignee/Labels, Add link, peek/full-page, 3 Copy rows, Delete). Rewrote it to match Linear 1:1 for everything we can back with real data: the six property rows now show shortcut hints + chevrons and open the shared pickers — **Due date** (new, `DatePicker` → `setIssueDueDate`) and **Project** (new, `ProjectPicker` → `setIssueProject`) added; hover-flyouts for **More properties** (Add link… ⌃L), **Create related** (Issue / Sub-issue ⌘⇧O / Parent / Blocked / Blocking — `createIssue`+link), **Mark as** (six relation kinds → `openRelationPicker`), **Copy** (ID ⌘. / URL ⌘⇧, / git branch ⌘⇧.); plus **Favorite/Unfavorite** (⌥F, `toggleFavorite`), **Subscribe/Unsubscribe** (⇧S, `toggleIssueSubscriber`), **Delete** (⌘⌫). Reuses the same `SubRow` flyout pattern + relation/copy surfaces as the ⋯ `IssueOptionsMenu`, keeping both menus in lockstep. Verified live (vite :5188): menu renders in Linear's exact order with the Mark-as flyout matching the screenshot, picking Jun 25 from the Due-date calendar stamps "Jun 25" on CLA-1, console clean; `tsc -b` + `npm run build` green. _(Convert to / Open in / Remind me omitted — need convert flow / coding-tool integration / reminder backend we don't have yet.)_
- [x] 🔴 **Documents** — Linear's workspace Documents feature. A `Document` model (`title`/`icon`/`content`/`projectId`/`creatorId`/timestamps) + `documents` store slice with `createDocument`/`updateDocument`/`deleteDocument` (persist-merge backfill, seeded "Product requirements" linked to MVP Launch + "Brand guidelines"). New `/documents` list view (icon + title + "Updated {ago} by {name}" rows, optional project chip, "New document" button, EmptyState with a create action) and `/document/:id` editor (40px emoji icon with a quick-pick popover, inline title input, "{author} · updated {ago}" meta, an inline `ProjectPicker`, the shared `MarkdownEditor` body, delete-with-confirm). Sidebar "Documents" entry in Workspace, ⌘K "Go to Documents" + "Create new document". Verified live (preview): list + editor render Markdown (headings/bold/bullets/task checkboxes), New document creates + opens a blank doc, console clean; both themes checked.
- [x] 🟡 **Team Estimates settings** — soi'd Linear's per-team **Estimates** setting: an `EstimationType` (`notUsed` / `linear` 0-5 / `exponential` 0,1,2,4,8,16 / `fibonacci` / `tshirt` XS-XL) + **Allow zero** on `Team`, a `setTeamEstimation` store action, and pure `estimatePoints(team)` / `estimateLabel(value, team)` / `teamEstimationType()` helpers in `constants.ts`. The issue **Estimate** property picker (shared by detail + peek) now uses the team's scale — t-shirt renders XS/S/M/L/XL, `notUsed` hides the row entirely; "Allow zero" toggles whether 0 is offered. Defaults fibonacci (matches the prior fixed scale). Verified live: settings page shows "Fibonacci · 0, 1, 2, 3, 5, 8" + Allow-zero off, issue estimate row renders, console clean.
- [x] 🟡 **Team Cycles settings + per-team Settings page** — restructured "Your teams → {team}" from a bare Workflow-states page into a proper team settings page (Linear-style): **General** (icon + name + issue count, read-only **Identifier** chip), **Estimates** (the new estimation type + allow-zero), **Cycles** (an **Enable cycles** toggle wired to `Team.cyclesEnabled` + `setTeamCyclesEnabled`), and **Workflow** (statuses). Disabling cycles now hides that team's **Cycles** entry from the sidebar (verified: toggling CLA off drops its sidebar Cycles row, ENG's stays). Verified live: toggle on/off works, sidebar updates, console clean.
- [x] 🟡 **Duplicate issue** — a `duplicateIssue(id)` store action clones an issue's core properties into a fresh same-team identifier (new `CLA-N`, own subscriber/creator, no copied comments/relations/sub-issues, cleared completed/canceled/triage — matching Linear's default) and a **Duplicate** row in the right-click `IssueContextMenu` (above Delete) that creates the copy and opens it. Verified: `duplicateIssue` mirrors the proven `createIssue` path; tsc + build green.

## Loop #85 — shipped (settings parity + Insights)

Every previously-stubbed ("Coming soon") Settings page is now a real, faithful
Linear surface, plus a new workspace **Insights** analytics view.

- [x] 🟡 **Settings → Integrations** — searchable grid of ~11 integration cards (GitHub, GitLab, Slack, Figma, Sentry, Zendesk, Discord, Notion, Google, Zapier, Jira) with Connect/Connected state persisted to `featureSettings`.
- [x] 🟡 **Settings → Billing** — current-plan card (Business, seats-used bar driven by real `users.length`), Monthly/Annually cycle toggle, Seats, Payment method, Billing history.
- [x] 🟡 **Settings → API** — personal API keys list (create reveals token once / revoke) + webhooks section.
- [x] 🟡 **Settings → Security (admin)** — auth (2FA/SAML/SCIM), access (allowed domains / public sharing), session controls — toggles persisted to `featureSettings`.
- [x] 🟡 **Settings → Connected accounts** — link GitHub/Google/Slack/Figma, persisted connect state.
- [x] 🟢 **Settings → Emojis** — custom-emoji grid with search + inline add/delete.
- [x] 🟡 **Settings → SLAs** — master enable gating SLA priority→target rules (add/remove).
- [x] 🟡 **Settings → Applications** — authorized OAuth apps list (revoke) + developer section.
- [x] 🟡 **Settings → Code & reviews** — connected code host + PR-automation toggles + branch format.
- [x] 🟡 **Settings → Security & access (personal)** — 2FA, passkeys list, active sessions, password.
- [x] 🟡 **Settings → Agent personalization** — assistance toggles, response tone, custom instructions.
- [x] 🟡 **Settings → AI & Agents** — AI feature toggles + agents list (Active/Paused).
- [x] 🟡 **Settings → Customer requests** — master enable → sources + ARR-weighted prioritization.
- [x] 🟡 **Settings → Releases** — master enable → versioned releases list + changelog toggles.
- [x] 🟡 **Settings → Pulse** — AI digest cadence/content toggles + preview card.
- [x] 🟡 **Settings → Asks** — internal help-desk channels, request types, automation.
- [x] 🟡 **Settings → Project templates** — project template list (new/duplicate/delete).
- [x] 🟡 **Settings → Project updates** — update reminder cadence + delivery + health requirement.
- [x] 🟢 **Settings → Project labels** — project-scoped label manager (search + create/delete + swatches).
- [x] 🟡 **Settings → Initiatives** — enable + display/update-cadence options.
- [x] 🟡 **Settings → Documents** — enable + permissions + editing options.
- [x] 🔴 **Insights** — workspace analytics view (`/insights`): summary stats (total/completed/in-progress/backlog/points + completion %) and By status / priority / assignee / project / label breakdown bars with a team filter; reachable from the sidebar Workspace section and ⌘K ("Go to Insights").

## Loop #86 — shipped (20 features: new surfaces + issue-detail sections + 2 modals)

New workspace/team surfaces, a Customers CRM-lite, issue Development/Attachments/
Reactions/Customer-requests/Similar sections, and Share/Move-to-team modals.

**Wave 1 — workspace/team views:**
- [x] 🟡 **Active Cycles overview** (`/cycles`) — cross-team active+upcoming cycle dashboard with progress.
- [x] 🟡 **Pulse** (`/pulse`) — workspace-wide activity feed (activities+comments+project/initiative updates), day-bucketed, All/Issues/Comments/Projects filter, peek on click.
- [x] 🟡 **Label issues view** (`/label/:id`) — all issues carrying a label (or its group's children), grouped by status; reachable from Labels settings "{n} issues".
- [x] 🟡 **Team Overview** (`/team/:key/overview`) — team home dashboard (stats, active cycle, status breakdown, members, projects, recent issues); sidebar "Overview" per team.

**Wave 2 — Customers + Members + issue sections:**
- [x] 🔴 **Customers** (`/customers`) — CRM-lite list (square tiles, tier chips, ARR, request counts, owner), ARR/Name/Requests sort, New-customer modal. Model `Customer` + `Issue.customerIds`.
- [x] 🔴 **Customer detail** (`/customer/:id`) — editable header, Requests list (add/unlink existing issues), Properties (Tier/Owner/ARR/Requests/Created), delete.
- [x] 🟡 **Releases** (`/releases`) — status-grouped release list (version chip, project chip, status picker, dates), New-release modal. Model `Release`.
- [x] 🟡 **Members directory** (`/members`) — searchable, role-filterable people directory with teams + assigned counts + summary.
- [x] 🟡 **Issue attachments** — collapsible "Attachments" section on the issue (kind icon, link, size, time, add/remove). Model `Attachment`.
- [x] 🟡 **Issue reactions** — emoji reactions on the issue itself (pills + picker), `Issue.reactions`.
- [x] 🟡 **Issue customer requests** — "Customers" section linking an issue to customers.

**Wave 3 — modals + changelog + projects board:**
- [x] 🟡 **Share issue modal** — Share dialog (copy link, Public-access toggle + embed), via ⋯ menu.
- [x] 🟡 **Move issue to team** — re-keys the issue into the destination team's sequence (new identifier), via ⋯ menu; follows the URL on the detail route.
- [x] 🟢 **Changelog** (`/changelog`) — "What's shipped" timeline (released releases + recently completed issues).
- [x] 🟡 **Projects board layout** — kanban of project cards in status columns (Display → Layout → Board).

**Wave 4 — profile + timeline + dev/similar sections:**
- [x] 🟡 **Profile / Your work** (`/profile`) — personal dashboard (stats, open-by-status, recent activity).
- [x] 🟡 **Projects Timeline layout** — Gantt-style start→target bars on a month axis (Display → Layout → Timeline).
- [x] 🟡 **Issue Development section** — linked PRs/branches (status pill, branch, author, status change/remove). Model `PullRequest`.
- [x] 🟢 **Similar issues** — suggested issues by title-word overlap (read-only, hidden when none).
- [x] 🟢 **Copy as Markdown** — issue ⋯ → Copy → Copy as Markdown.

## Loop #87 — shipped

New nav/lifecycle surfaces, issue-detail & board enhancements, settings pages, reminders.

**Wave 1 — workspace nav + issue lifecycle:**
- [x] 🟡 **Archive** (`/archive`) — archived issues grouped by team; Restore / Delete; `Issue.archivedAt`, `archiveIssue`/`unarchiveIssue`, excluded from all active lists. Archive action in the issue ⋯ menu.
- [x] 🟡 **Recently viewed** (`/recent`) — issues you opened, newest first; `recentIssueIds` slice + `pushRecentIssue` (tracked on IssueDetail mount).
- [x] 🟢 **Favorites page** (`/favorites`) — starred issues/projects/views grouped, unstar inline.
- [x] 🟡 **Teams directory** (`/teams`) — all teams with member avatars + issue/active/project counts, click → overview.
- [x] 🔴 **All issues** (`/all-issues`) — workspace-wide issue list across every team (FilterBar + Display options + board/list), like IssuesView without team scope.

**Wave 2 — graphs, reminders, labels:**
- [x] 🟡 **Project progress graph** — a "Graph" tab on the project detail: burn-up SVG (Scope vs cumulative Completed over the project lifespan, today marker, gridlines, legend), mirroring CycleBurndown's idiom.
- [x] 🟢 **Initiative progress graph** — "Progress by project" roll-up card on the initiative overview (aggregate done/total + per-project bars tinted by project color).
- [x] 🟡 **Issue reminders** — a "Reminder" property row (Bell): quick presets (In 1 hour / This evening / Tomorrow / Next week) + Remove; `Issue.remindAt`, `setIssueReminder`.
- [x] 🟡 **Reminders view** (`/reminders`) — issues with a reminder, split Overdue / Upcoming, dismiss inline.
- [x] 🟢 **Labels directory** (`/labels`) — all labels (groups + children + ungrouped) with per-label issue counts, click → `/label/:id`.

**Wave 3 — settings pages (model: featureValues KV slice):**
- [x] 🟡 **Estimates settings** — per-team estimation scale (type from ESTIMATION_TYPES + allow-zero), wired to `setTeamEstimation`.
- [x] 🟡 **Cycles settings** — per-team enable + cadence (length / cooldown / start day / upcoming count / auto-add / auto-roll) via `setTeamCyclesEnabled` + featureValues/featureSettings.
- [x] 🟡 **Triage settings** — per-team enable triage + responsibility + auto-assign + notify.
- [x] 🟡 **Import settings** — import-sources page (Jira/Asana/GitHub/Shortcut/Trello/Linear/CSV/Height) with mock toast + persisted "imported" state.
- [x] 🔴 **Audit log** — workspace audit log driven by real `activities` (actor + sentence + issue link + time), actor/type filters, CSV export.

**Wave 4 — board grouping + issue/project sections (model: Project.resources):**
- [x] 🔴 **Board group-by** — the kanban board now honors the Display "Columns" grouping (Status / Assignee / Priority / Project), with drag-to-set-the-grouped-property; label-grouping falls back to status (multi-label would dup DnD ids). Wired into IssuesView / AllIssuesView / MyIssues / SavedViewScreen.
- [x] 🟡 **Activity feed filter** — All / Comments / Updates segmented control on the issue activity feed.
- [x] 🟡 **Project resources** — a "Resources" section on the project Overview: add/remove external links (Figma/docs/specs), stored on `Project.resources` via `updateProject`.

**Wave 5 — issue lifecycle actions:**
- [x] 🟡 **Bulk archive** — an Archive action in the floating bulk-action bar (`bulkArchive`).
- [x] 🟢 **Duplicate issue** (⋯ menu) — wires the existing `duplicateIssue` into the issue options header menu, opening the copy.
- [x] 🟢 **Archive from right-click** — Archive / Restore row in the issue context menu.

## Loop #88 — shipped (bulk actions + grouping + shell/display polish)

The app is at deep Linear parity; this run filled real cross-cutting gaps in the
bulk-action bar, grouping, the app shell, and issue-row display properties.

**Wave 1 — bulk-action bar + grouping (model: bulk* store actions):**
- [x] 🟡 **Bulk set Project** — `bulkSetProject` (clears the now-dangling milestone) + a Project picker in the floating bulk bar.
- [x] 🟡 **Bulk set Cycle** — `bulkSetCycle` + a Cycle picker (hidden when no cycles exist).
- [x] 🟡 **Bulk set Due date** — `bulkSetDueDate` + the shared `DatePicker` (stores ISO, matching the single-issue path).
- [x] 🟡 **Bulk set Estimate** — `bulkSetEstimate` + a 1/2/3/5/8 picker.
- [x] 🟡 **Bulk Subscribe / Unsubscribe** — `bulkSubscribe(ids, on)` toggling the current user across the selection (label flips when all are already subscribed).
- [x] 🟢 **Bulk Favorite / Unfavorite** — `bulkFavorite(ids, on)` adds/removes issue favorites (real two-way toggle).
- [x] 🟡 **Group by Cycle** — `groupIssues` cycle branch + `IterationCw` group glyph; board falls back to status columns via `boardColumnGroupBy`.
- [x] 🟡 **Group by Milestone** — `groupIssues` milestone branch + `Diamond` glyph; same board fallback. Both wired into IssuesView / AllIssuesView / MyIssues / SavedViewScreen (columns + sub-grouping + swimlanes).

**Wave 2 — app shell + issue lifecycle:**
- [x] 🟡 **Collapsible sidebar** — wires the long-existing `sidebarCollapsed`/`toggleSidebar`: a `PanelLeftClose` button in the workspace header collapses the sidebar (content shifts right with a floating `PanelLeft` expand button); **⌘/** toggles it; documented in the `?` help overlay.
- [x] 🟡 **Convert sub-issue to issue** — a "Convert to issue" row (shown only when the issue has a parent) in both the ⋯ `IssueOptionsMenu` and the right-click `IssueContextMenu`, calling `setIssueParent(id, undefined)`.

**Wave 3 — roadmap + command menu:**
- [x] 🟢 **Roadmap zoom** — Compact / Default / Wide per-month-column widths (72/120/200px) via a segmented control in the Roadmap header.
- [x] 🟡 **⌘K Switch team** — per-team "Switch to {team}" commands in the command menu.
- [x] 🟢 **⌘K Toggle sidebar** — a "Toggle sidebar" command (⌘/ hint) in the command menu.

**Wave 4–5 — issue-row display properties:**
- [x] 🟡 **Comment-count indicator** — a `MessageSquare` + count on issue rows whenever an issue has comments (always-on, like Linear).
- [x] 🟢 **Estimate display property** — a new `estimate` Display-options pill (default off) rendering a `Gauge` + team-aware estimate (points / t-shirt) on issue rows.

## Loop #89 — shipped (24 features: list filter/sort/export depth + interactions + project dependencies)

Deepened filter/sort/export/interaction capabilities across nearly every list
surface, plus one new data feature (project dependencies), then 4 bug fixes.

**Wave 1 — search/insights/feeds/board/customers + list interactions:**
- [x] 🟡 **Search entity tabs + Documents** — All/Issues/Projects/Documents pills (counts) + document title/content search → /document/:id.
- [x] 🟡 **Insights time-range + CSV export** — All/7/30/90-day range filter + Export-CSV of the chosen breakdown (RFC-quoted).
- [x] 🟡 **Pulse team + date-scope filters** — team dropdown (per-event resolved) + All time/Today/This week scope.
- [x] 🟡 **Archive search + team filter** — identifier/title search + team dropdown, AND-composed, no-matches empty state.
- [x] 🟡 **Board card peek** — clicking a board card opens the peek panel (setPeek), not full-page; drag still suppresses the click.
- [x] 🟡 **Customers tier filter** — All/Free/Startup/Business/Enterprise pills + total-ARR summary over the filtered set.
- [x] 🟡 **Shift-range select** — shift-click selects the inclusive row range via the list's visible order (navIssueIds).
- [x] 🟢 **j/k re-peek** — ↓/↑ advance the open peek panel to the next/prev issue; documented in the `?` overlay.

**Wave 2 — directory/list sort + filter + reschedule:**
- [x] 🟡 **Members sort** — Name/Assigned/Active/Teams with an active-issue count per row.
- [x] 🟡 **Releases status filter + progress** — status pill row + per-release done/total donut from the linked project.
- [x] 🟡 **Documents sort + project filter** — Last updated/Created/Title + All/each project/No project.
- [x] 🟢 **Labels directory search + sort** — name search + Name A→Z / Most used.
- [x] 🟡 **Reminders inline reschedule** — clock Popover (In 1h / This evening / Tomorrow / Next week / Remove) → setIssueReminder, auto re-sort.
- [x] 🟢 **Recent search + team filter** — identifier/title search + team dropdown, newest-first preserved.
- [x] 🟡 **Teams directory search + sort** — name/key search + Name/Members/Issues/Active.
- [x] 🟡 **Initiatives sort + owner filter** — Name/Progress/Target/Projects + owner dropdown, per tab.

**Wave 3 — roadmap/changelog/cycles/favorites/profile/views/triage + dependencies:**
- [x] 🟡 **Roadmap status + initiative filters** — narrow bars by status and initiative, empty state when all hidden.
- [x] 🟡 **Changelog type + team filters** — All/Releases/Completed issues + team dropdown.
- [x] 🟡 **Active-Cycles team filter + scope toggle** — All/per-team + Active & upcoming / Past (final progress).
- [x] 🟡 **Favorites type-pill + search** — All/Issues/Projects/Views (counts) + search, no-matches empty state.
- [x] 🟡 **Profile time-period toggle** — All time/This week/This month scoping stats + recent activity.
- [x] 🟢 **Views search + sort** — name search + Name A→Z / Recently created.
- [x] 🟡 **Triage priority filter + sort** — priority dropdown + Newest/Oldest/Priority.
- [x] 🔴 **Project dependencies** — `Project.dependsOn` + cycle-guarded add/remove actions; a Dependencies section on the project Overview (Blocked by / Blocking lists, project picker, inline remove); seeded Mobile App ← MVP Launch.
